---
layout: post
title: "json"
description: "json은 무엇이고, python에서 이를 어떻게 사용하는가?"
category: Python
---

# json

## json이 무엇인가?
json은 텍스트 형식이다.

보통 데이터를 저장하거나 주고받을 때 많이 사용한다.

예를 들면 이런 형태를 가진다.

```json
{
  "name": "조세진",
  "age": 20,
  "major": "AI소프트웨어학부"
}
```

파이썬에서는 보통 다음과 같은 데이터와 연결된다고 보면 된다.

```python
student = {
    "name": "조세진",
    "age": 20,
    "major": "AI소프트웨어학부"
}
```

json은 이런 딕셔너리 같은 데이터를 파일로 저장하거나, 다른 프로그램과 주고 받기 좋은 텍스트 형태이다.

## python에서 어떻게 사용하는가?

### json.dumps()란?
`json.dumps()`는 파이썬 데이터를 json 문자열로 변환해주는 것이다.

```python
import json

student = {
    "name": "조세진",
    "age": 20,
    "major": "AI소프트웨어학부"
}

json_text = json.dumps(student, ensure_ascii=False, indent=2)

print(json_text)
print(type(json_text))
```

```
{
  "name": "조세진",
  "age": 20,
  "major": "AI소프트웨어학부"
}
```

### json.loads()
`json.dumps()`는 json 문자열을 파이썬 데이터로 변환해주는 것이다.

```python
import json

json_text = '{"name": "조세진", "age": 20}'

student = json.loads(json_text)

print(student)
print(type(student))
print(student["name"])
```

```python
{'name': '조세진', 'age': 20}
<class 'dict'>
조세진
```

json 문자열의 모양에 따라 파이썬 타입이 달라진다.

```python
import json

print(json.loads('[1, 2, 3]'))
print(type(json.loads('[1, 2, 3]')))
```

```python
[1, 2, 3]
<class 'list'>
```

즉, json문자열을 보고 알맞는 파이썬 자료형으로 바꿔주는 것이다.

### `json.dump()`란?
파이썬 데이터를 json파일로 저장해주는 것이다.

아까 `dumps()`는 파이썬 데이터를 json문자열로 저장해주는 것이다.
